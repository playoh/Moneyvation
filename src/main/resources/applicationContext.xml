<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 1. DB 연결 정보 (방금 주신 학교 서버 정보로 수정됨) -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="org.mariadb.jdbc.Driver" />
        <property name="url" value="jdbc:mariadb://walab.handong.edu:3306/W25_22400412" />
        <property name="username" value="W25_22400412" />
        <property name="password" value="Uife7t" />
    </bean>

    <!-- 2. MyBatis 설정 (이게 있어야 SQL을 실행합니다!) -->
    <!-- JdbcTemplate 대신 SqlSessionFactory를 써야 합니다 -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <!-- 매퍼 파일 위치가 정확한지 꼭 확인하세요 -->
        <property name="mapperLocations" value="classpath:mappers/*-mapper.xml"/>
    </bean>

    <bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate" destroy-method="clearCache">
        <constructor-arg name="sqlSessionFactory" ref="sqlSessionFactory"/>
    </bean>

    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="org.example.moneyvation.dao"/>
    </bean>
</beans>
