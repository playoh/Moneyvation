<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. Spring MVC 설정 (기존 유지) -->
    <mvc:annotation-driven />

    <!-- 패키지명은 본인 프로젝트에 맞게 수정 (중요!) -->
    <context:component-scan base-package="org.example.moneyvation" />

    <!-- 정적 리소스 경로 (resources 폴더 없으면 경고 뜨지만 실행엔 문제 없음) -->
    <mvc:resources mapping="/resources/**" location="/resources/" />
    <mvc:resources mapping="/img/**" location="/resources/img/" />

    <!-- View Resolver (기존 유지) -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver" >
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- ========================================== -->
    <!--           새로 추가된 DB/MyBatis 설정       -->
    <!-- ========================================== -->

    <!-- 2. DB 연결 정보 (비밀번호 입력 필수!) -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="org.mariadb.jdbc.Driver" />
        <property name="url" value="jdbc:mariadb://walab.handong.edu:3306/W25_22400412" />
        <property name="username" value="W25_22400412" />
        <property name="password" value="Uife7t" />
    </bean>

    <!-- 3. MyBatis 핵심 객체 -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <!-- Mapper XML 위치 -->
        <property name="mapperLocations" value="classpath:mappers/*.xml" />
        <!-- MyBatis 설정 파일 위치 -->
        <property name="configLocation" value="classpath:mybatis-config.xml" />
    </bean>

    <!-- 4. 쿼리 실행 템플릿 -->
    <bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate">
        <constructor-arg ref="sqlSessionFactory" />
    </bean>

    <!-- 5. Mapper 인터페이스 자동 스캔 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="org.example.moneyvation.dao" />
    </bean>

</beans>
