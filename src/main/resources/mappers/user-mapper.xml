<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.moneyvation.dao.UserMapper">

    <select id="getUser" resultType="user">
        SELECT
            user_id as userId,
            password,
            user_name as userName
        FROM users
        WHERE user_id = #{userId}
    </select>

    <insert id="insertUser">
        INSERT INTO users (user_id, password, name, nickname, created_at)
        VALUES (#{userId}, #{password}, #{userName}, #{nickname}, NOW())    </insert>

</mapper>